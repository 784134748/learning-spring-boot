<?xml version='1.0' encoding='UTF-8' ?>
<!DOCTYPE mapper PUBLIC '-//mybatis.org//DTD Mapper 3.0//EN' 'http://mybatis.org/dtd/mybatis-3-mapper.dtd' >
<mapper namespace='com.yalonglee.learning.security.mapper.SysRoleUserMapper'>
    <sql id='Base_Column_List' >
    id, role_id, user_id, operator, operate_time, operate_ip
    </sql>
    <select id='selectByPrimaryKey' resultType='com.yalonglee.learning.security.model.SysRoleUser' parameterType='java.lang.Long'>
        select 
        <include refid= 'Base_Column_List' />
        from sys_role_user 
        where id = #{id}
    </select>

    <select id='selectByQuery' resultType='com.yalonglee.learning.security.model.SysRoleUser' parameterType='java.util.Map'>
        select 
        <include refid= 'Base_Column_List' />
        from sys_role_user 
        <where>
            <include refid='query_filter'/>
        </where>
        <if test='start != null'>
            LIMIT #{start},#{limit}
        </if>
    </select>

    <delete id='deleteByPrimaryKey' parameterType='java.lang.Long'>
        delete from sys_role_user where id = #{id}
    </delete>

    <select id='count' resultType='java.lang.Integer' parameterType='java.util.Map'>
        select count(*) from sys_role_user
        <where>
            <include refid='query_filter'/>
        </where>
    </select>

    <insert id='insert' parameterType='java.util.Map' useGeneratedKeys='true' keyProperty='id'>
        insert into sys_role_user
        <trim prefix='(' suffix=')' suffixOverrides=','>
                <if test='id != null'>id,</if>
                <if test='roleId != null'>role_id,</if>
                <if test='userId != null'>user_id,</if>
                <if test='operator != null'>operator,</if>
                <if test='operateTime != null'>operate_time,</if>
                <if test='operateIp != null'>operate_ip,</if>
        </trim>
        <trim prefix='values (' suffix=')' suffixOverrides=','>
                <if test='id != null'>#{id},</if>
                <if test='roleId != null'>#{roleId},</if>
                <if test='userId != null'>#{userId},</if>
                <if test='operator != null'>#{operator},</if>
                <if test='operateTime != null'>#{operateTime},</if>
                <if test='operateIp != null'>#{operateIp},</if>
        </trim>
    </insert>

    <update id='update' parameterType='java.util.Map'>
        update sys_role_user
        <set>
                <if test='id != null'>id = #{id},</if>
                <if test='roleId != null'>role_id = #{roleId},</if>
                <if test='userId != null'>user_id = #{userId},</if>
                <if test='operator != null'>operator = #{operator},</if>
                <if test='operateTime != null'>operate_time = #{operateTime},</if>
                <if test='operateIp != null'>operate_ip = #{operateIp},</if>
        </set>
        where id = #{id}
    </update>

    <sql id='query_filter'>
            <if test='id != null'>and id = #{id}</if>
            <if test='roleId != null'>and role_id = #{roleId}</if>
            <if test='userId != null'>and user_id = #{userId}</if>
            <if test='operator != null'>and operator = #{operator}</if>
            <if test='operateTime != null'>and operate_time = #{operateTime}</if>
            <if test='operateIp != null'>and operate_ip = #{operateIp}</if>
    </sql>


</mapper>
