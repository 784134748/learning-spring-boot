<?xml version='1.0' encoding='UTF-8' ?>
<!DOCTYPE mapper PUBLIC '-//mybatis.org//DTD Mapper 3.0//EN' 'http://mybatis.org/dtd/mybatis-3-mapper.dtd' >
<mapper namespace='com.yalonglee.learning.security.mapper.SysUserMapper'>
    <sql id='Base_Column_List' >
    id, username, telephone, mail, password, dept_id, status, remark, operator, operate_time, operate_ip
    </sql>
    <select id='selectByPrimaryKey' resultType='com.yalonglee.learning.security.model.SysUser' parameterType='java.lang.Long'>
        select 
        <include refid= 'Base_Column_List' />
        from sys_user 
        where id = #{id}
    </select>

    <select id='selectByQuery' resultType='SysUser' parameterType='java.util.Map'>
        select 
        <include refid= 'Base_Column_List' />
        from sys_user 
        <where>
            <include refid='query_filter'/>
        </where>
        <if test='start != null'>
            LIMIT #{start},#{limit}
        </if>
    </select>

    <delete id='deleteByPrimaryKey' parameterType='java.lang.Long'>
        delete from sys_user where id = #{id}
    </delete>

    <select id='count' resultType='java.lang.Integer' parameterType='java.util.Map'>
        select count(*) from sys_user
        <where>
            <include refid='query_filter'/>
        </where>
    </select>

    <insert id='insert' parameterType='java.util.Map' useGeneratedKeys='true' keyProperty='id'>
        insert into sys_user
        <trim prefix='(' suffix=')' suffixOverrides=','>
                <if test='id != null'>id,</if>
                <if test='username != null'>username,</if>
                <if test='telephone != null'>telephone,</if>
                <if test='mail != null'>mail,</if>
                <if test='password != null'>password,</if>
                <if test='deptId != null'>dept_id,</if>
                <if test='status != null'>status,</if>
                <if test='remark != null'>remark,</if>
                <if test='operator != null'>operator,</if>
                <if test='operateTime != null'>operate_time,</if>
                <if test='operateIp != null'>operate_ip,</if>
        </trim>
        <trim prefix='values (' suffix=')' suffixOverrides=','>
                <if test='id != null'>#{id},</if>
                <if test='username != null'>#{username},</if>
                <if test='telephone != null'>#{telephone},</if>
                <if test='mail != null'>#{mail},</if>
                <if test='password != null'>#{password},</if>
                <if test='deptId != null'>#{deptId},</if>
                <if test='status != null'>#{status},</if>
                <if test='remark != null'>#{remark},</if>
                <if test='operator != null'>#{operator},</if>
                <if test='operateTime != null'>#{operateTime},</if>
                <if test='operateIp != null'>#{operateIp},</if>
        </trim>
    </insert>

    <update id='update' parameterType='java.util.Map'>
        update sys_user
        <set>
                <if test='id != null'>id = #{id},</if>
                <if test='username != null'>username = #{username},</if>
                <if test='telephone != null'>telephone = #{telephone},</if>
                <if test='mail != null'>mail = #{mail},</if>
                <if test='password != null'>password = #{password},</if>
                <if test='deptId != null'>dept_id = #{deptId},</if>
                <if test='status != null'>status = #{status},</if>
                <if test='remark != null'>remark = #{remark},</if>
                <if test='operator != null'>operator = #{operator},</if>
                <if test='operateTime != null'>operate_time = #{operateTime},</if>
                <if test='operateIp != null'>operate_ip = #{operateIp},</if>
        </set>
        where id = #{id}
    </update>

    <sql id='query_filter'>
            <if test='id != null'>and id = #{id}</if>
            <if test='username != null'>and username = #{username}</if>
            <if test='telephone != null'>and telephone = #{telephone}</if>
            <if test='mail != null'>and mail = #{mail}</if>
            <if test='password != null'>and password = #{password}</if>
            <if test='deptId != null'>and dept_id = #{deptId}</if>
            <if test='status != null'>and status = #{status}</if>
            <if test='remark != null'>and remark = #{remark}</if>
            <if test='operator != null'>and operator = #{operator}</if>
            <if test='operateTime != null'>and operate_time = #{operateTime}</if>
            <if test='operateIp != null'>and operate_ip = #{operateIp}</if>
    </sql>


</mapper>
